# Core runtime dependencies
requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0            # fast parser; the scraper falls back to html.parser without it

# Optional, but the most robust answer to TLS fingerprinting (see README).
# Without it the scraper falls back to the system `curl` binary.
# curl_cffi>=0.7

# For the Python port of the motion model (python_port/) and its tests
numpy>=1.26

# Development / test
pytest>=8.0
